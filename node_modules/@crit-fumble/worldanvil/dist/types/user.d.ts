/**
 * World Anvil User Types
 *
 * Types for user identity and authentication.
 */
import type { EntityRef, Timestamps } from './common.js';
/**
 * User reference (granularity -1)
 */
export interface UserRef extends EntityRef {
}
/**
 * User with basic info (granularity 0)
 */
export interface User extends UserRef, Timestamps {
    username?: string;
    avatar?: string;
    membershipType?: string;
}
/**
 * User with extended info (granularity 2)
 */
export interface UserFull extends User {
    email?: string;
    worlds?: EntityRef[];
    following?: number;
    followers?: number;
}
/**
 * Current authenticated user identity response
 */
export interface UserIdentityResponse {
    id: string;
    username: string;
    avatar?: string;
    membershipType: string;
}
//# sourceMappingURL=user.d.ts.map