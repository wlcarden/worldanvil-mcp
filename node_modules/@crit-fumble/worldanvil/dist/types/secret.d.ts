/**
 * World Anvil Secret Types
 *
 * Types for secrets - hidden content for GMs.
 */
import type { EntityRef, Timestamps } from './common.js';
import type { WorldRef } from './world.js';
import type { ArticleRef } from './article.js';
/**
 * Secret reference (granularity -1)
 */
export interface SecretRef extends EntityRef {
}
/**
 * Secret with basic info (granularity 0)
 */
export interface Secret extends SecretRef, Timestamps {
    /** Secret content (BBCode/markdown) */
    content?: string;
    /** World this secret belongs to */
    world?: WorldRef;
    /** Articles this secret is attached to */
    articles?: ArticleRef[];
    /** Is this secret revealed */
    isRevealed?: boolean;
}
/**
 * Request to create a new secret
 */
export interface CreateSecretRequest {
    title: string;
    world: {
        id: string;
    };
    content?: string;
    articles?: {
        id: string;
    }[];
}
/**
 * Request to update a secret
 */
export interface UpdateSecretRequest {
    title?: string;
    content?: string;
    articles?: {
        id: string;
    }[];
    isRevealed?: boolean;
}
/**
 * Response for secret list
 */
export interface SecretListResponse {
    entities: SecretRef[];
}
//# sourceMappingURL=secret.d.ts.map