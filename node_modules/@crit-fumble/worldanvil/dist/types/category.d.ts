/**
 * World Anvil Category Types
 *
 * Types for article categories.
 */
import type { EntityRef, Timestamps } from './common.js';
import type { WorldRef } from './world.js';
/**
 * Category reference (granularity -1)
 */
export interface CategoryRef extends EntityRef {
}
/**
 * Category with basic info (granularity 0)
 */
export interface Category extends CategoryRef, Timestamps {
    /** Category description */
    description?: string;
    /** World this category belongs to */
    world?: WorldRef;
    /** Parent category for nested structure */
    parent?: CategoryRef;
    /** Position/order */
    position?: number;
    /** Icon or image URL */
    icon?: string;
}
/**
 * Category with articles (granularity 2)
 */
export interface CategoryFull extends Category {
    /** Child categories */
    children?: CategoryRef[];
    /** Number of articles in this category */
    articleCount?: number;
}
/**
 * Request to create a new category
 */
export interface CreateCategoryRequest {
    title: string;
    world: {
        id: string;
    };
    description?: string;
    parent?: {
        id: string;
    };
    position?: number;
}
/**
 * Request to update a category
 */
export interface UpdateCategoryRequest {
    title?: string;
    description?: string;
    parent?: {
        id: string;
    };
    position?: number;
}
/**
 * Response for category list
 */
export interface CategoryListResponse {
    entities: CategoryRef[];
}
//# sourceMappingURL=category.d.ts.map