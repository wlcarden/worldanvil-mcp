/**
 * World Anvil Article Types
 *
 * Types for articles - the main content units in World Anvil.
 * Articles can be characters, locations, items, organizations, etc.
 */
import type { EntityRef, EntityState, Timestamps } from './common.js';
import type { WorldRef } from './world.js';
import type { UserRef } from './user.js';
/**
 * Article template types
 */
export type ArticleTemplate = 'generic' | 'person' | 'character' | 'location' | 'settlement' | 'building' | 'landmark' | 'geography' | 'item' | 'vehicle' | 'organization' | 'military' | 'formation' | 'rank' | 'technology' | 'material' | 'condition' | 'document' | 'law' | 'language' | 'ethnicity' | 'species' | 'ritual' | 'myth' | 'tradition' | 'spell' | 'plot' | 'report' | 'prose' | string;
/**
 * Article reference (granularity -1)
 */
export interface ArticleRef extends EntityRef {
    /** Article template type */
    entityClass?: ArticleTemplate;
}
/**
 * Article with basic info (granularity 0)
 */
export interface Article extends ArticleRef, Timestamps, EntityState {
    /** Article excerpt/summary */
    excerpt?: string;
    /** Cover image URL */
    cover?: string;
    /** Portrait image URL */
    portrait?: string;
    /** World this article belongs to */
    world?: WorldRef;
    /** Author of the article */
    author?: UserRef;
    /** Word count */
    wordcount?: number;
    /** Article tags */
    tags?: string[];
}
/**
 * Article with content (granularity 1)
 */
export interface ArticleWithContent extends Article {
    /** Full content (BBCode/markdown) */
    content?: string;
    /** Sidebar content */
    sidebar?: string;
    /** Category */
    category?: EntityRef;
    /** Parent article */
    parent?: ArticleRef;
}
/**
 * Article with relationships (granularity 2)
 */
export interface ArticleFull extends ArticleWithContent {
    /** Child articles */
    children?: ArticleRef[];
    /** Related articles */
    relations?: ArticleRef[];
    /** Articles that mention this one */
    mentions?: ArticleRef[];
    /** Secrets attached to this article */
    secrets?: EntityRef[];
}
/**
 * Request to create a new article
 */
export interface CreateArticleRequest {
    title: string;
    world: {
        id: string;
    };
    templateType?: ArticleTemplate;
    content?: string;
    excerpt?: string;
    tags?: string[];
    state?: 'private' | 'public';
    isDraft?: boolean;
    isWip?: boolean;
    category?: {
        id: string;
    };
    parent?: {
        id: string;
    };
}
/**
 * Request to update an article
 */
export interface UpdateArticleRequest {
    title?: string;
    content?: string;
    excerpt?: string;
    sidebar?: string;
    tags?: string[];
    state?: 'private' | 'public';
    isDraft?: boolean;
    isWip?: boolean;
    category?: {
        id: string;
    };
    parent?: {
        id: string;
    };
}
/**
 * Response for article list
 */
export interface ArticleListResponse {
    entities: ArticleRef[];
}
//# sourceMappingURL=article.d.ts.map