/**
 * World Anvil Timeline Types
 *
 * Types for timelines and historical events.
 */
import type { EntityRef, Timestamps } from './common.js';
import type { WorldRef } from './world.js';
/**
 * Timeline reference (granularity -1)
 */
export interface TimelineRef extends EntityRef {
}
/**
 * Timeline with basic info (granularity 0)
 */
export interface Timeline extends TimelineRef, Timestamps {
    /** Timeline description */
    description?: string;
    /** World this timeline belongs to */
    world?: WorldRef;
    /** Cover image URL */
    cover?: string;
    /** Number of eras */
    eraCount?: number;
    /** Number of events */
    eventCount?: number;
}
/**
 * Timeline with full info (granularity 2)
 */
export interface TimelineFull extends Timeline {
    /** Eras in this timeline */
    eras?: EntityRef[];
    /** Historical events */
    events?: EntityRef[];
}
/**
 * Request to create a new timeline
 */
export interface CreateTimelineRequest {
    title: string;
    world: {
        id: string;
    };
    description?: string;
}
/**
 * Request to update a timeline
 */
export interface UpdateTimelineRequest {
    title?: string;
    description?: string;
}
/**
 * Response for timeline list
 */
export interface TimelineListResponse {
    entities: TimelineRef[];
}
/**
 * History/Event reference (granularity -1)
 */
export interface HistoryRef extends EntityRef {
}
/**
 * Historical event with basic info (granularity 0)
 */
export interface History extends HistoryRef, Timestamps {
    /** Event description/content */
    content?: string;
    /** World this event belongs to */
    world?: WorldRef;
    /** Date of the event (in-world) */
    date?: string;
    /** Era this event belongs to */
    era?: EntityRef;
    /** Timeline this event belongs to */
    timeline?: TimelineRef;
    /** Event importance/significance */
    importance?: number;
}
/**
 * Request to create a new history event
 */
export interface CreateHistoryRequest {
    title: string;
    world: {
        id: string;
    };
    content?: string;
    date?: string;
    timeline?: {
        id: string;
    };
}
/**
 * Request to update a history event
 */
export interface UpdateHistoryRequest {
    title?: string;
    content?: string;
    date?: string;
    timeline?: {
        id: string;
    };
}
/**
 * Response for history list
 */
export interface HistoryListResponse {
    entities: HistoryRef[];
}
//# sourceMappingURL=timeline.d.ts.map