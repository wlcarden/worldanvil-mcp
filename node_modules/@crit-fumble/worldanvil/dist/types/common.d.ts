/**
 * World Anvil Common Types
 *
 * Shared types used across all World Anvil entities.
 */
/**
 * Granularity levels for API responses.
 * Controls the amount of data returned.
 *
 * - -1: Reference only (id, title, url)
 * - 0: Minimal display data
 * - 1: Principal display data
 * - 2: Detailed data with relationships
 * - 3: Full expanded data (rarely available)
 */
export type Granularity = -1 | 0 | 1 | 2 | 3;
/**
 * Entity reference (granularity -1)
 * Minimal reference to another entity
 */
export interface EntityRef {
    id: string;
    title?: string;
    url?: string;
}
/**
 * Timestamps common to most entities
 */
export interface Timestamps {
    creationDate?: string;
    updateDate?: string;
    publicationDate?: string;
}
/**
 * State information for entities
 */
export interface EntityState {
    state?: 'private' | 'public';
    isDraft?: boolean;
    isWip?: boolean;
}
/**
 * API success response
 */
export interface SuccessResponse {
    success: boolean;
}
/**
 * API error response
 */
export interface ErrorResponse {
    error: string;
    message?: string;
    code?: number;
}
//# sourceMappingURL=common.d.ts.map